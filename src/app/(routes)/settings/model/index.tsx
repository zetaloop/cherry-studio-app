import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from 'tamagui'

export default function ModelSettingsPage() {
  return (
    <SafeAreaView>
      <Text>ModelSettingsPage</Text>
    </SafeAreaView>
  )
}
