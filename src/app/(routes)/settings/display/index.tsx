import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from 'tamagui'

export default function DisplaySettingsPage() {
  return (
    <SafeAreaView>
      <Text>DisplaySettingsPage</Text>
    </SafeAreaView>
  )
}
