import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from 'tamagui'

export default function WebSearchSettingsPage() {
  return (
    <SafeAreaView>
      <Text>WebSearchSettingsPage</Text>
    </SafeAreaView>
  )
}
