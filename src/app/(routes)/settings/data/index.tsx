import { SafeAreaView } from 'react-native-safe-area-context'
import { Text } from 'tamagui'

export default function DataSettingsPage() {
  return (
    <SafeAreaView>
      <Text>DataSettingsPage</Text>
    </SafeAreaView>
  )
}
